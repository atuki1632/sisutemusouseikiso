#include <math.h>
#include <stdio.h>

double mysqrt(double x);

int main(void)
{
	double x;
	printf("x=");
	; scanf_s("%lf", &x);

	double plus = sqrt(x);

	if (x != 0) {
		printf("ans1 = %f\n",plus);
		printf("ans2 = %f\n",-plus);
	}else {
		printf("ans = %f\n",plus);
	}

	return 0;
}
